import type { Document } from "../../types"; interface SidebarProps { documents: Document[]; currentDocument: Document | null; onSelectDocument: (doc: Document) => void; loading: boolean; viewMode: "editor" | "kanban"; } function Sidebar({ documents, currentDocument, onSelectDocument, loading, viewMode }: SidebarProps) { const formatDate = (date: Date) => { return new Date(date).toLocaleDateString("zh-CN", { month: "short", day: "numeric" }); }; return (<aside className="sidebar"> <div className="sidebar-header"> <h2>Documents</h2> <span className="doc-count">{documents.length}</span> </div> <div className="sidebar-content"> {loading ? (<div className="skeleton-list"> <div className="skeleton-item"><div className="skeleton skeleton-title"></div><div className="skeleton skeleton-date"></div></div> <div className="skeleton-item"><div className="skeleton skeleton-title"></div><div className="skeleton skeleton-date"></div></div> <div className="skeleton-item"><div className="skeleton skeleton-title"></div><div className="skeleton skeleton-date"></div></div> </div>) : (<ul className="doc-list"> {documents.map((doc, index) => (<li key={doc._id} className={`doc-item ${currentDocument?._id === doc._id ? "active" : ""}`} onClick={() => onSelectDocument(doc)} style={{ animationDelay: `${index * 50}ms` }}> <div className="doc-card"> <h3 className="doc-title">{doc.title}</h3> <p className="doc-meta"> {formatDate(doc.updatedAt)} · {doc.versions.length} versions </p> <span className={`status-badge ${doc.status}`}> {doc.status === "draft" ? "Draft" : doc.status === "review" ? "Review" : "Completed"} </span> </div> </li>))} </ul>)} </div> </aside>); } export default Sidebar;
