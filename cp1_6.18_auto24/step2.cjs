const fs = require("fs"); fs.appendFileSync("./src/pages/profile.tsx", `const BadgeModal: React.FC<BadgeModalProps> = ({ open, badge, onClose }) => {\n  const [closing, setClosing] = useState(false);\n\n  useEffect(() => {\n    if (open) {\n      setClosing(false);\n    }\n  }, [open]);\n\n  const handleClose = () => {\n    setClosing(true);\n    setTimeout(() => {\n      onClose();\n    }, 240);\n  };\n\n  const handleOverlayClick = (e: React.MouseEvent<HTMLDivElement>) => {\n    if (e.target === e.currentTarget) {\n      handleClose();\n    }\n  };\n\n  if (!open || !badge) return null;\n\n`, "utf-8");